# OpenAI Agents SDK App Example

Use this shape when the application itself needs an agent runtime.

```text
src/agents/
  instructions/
  tools/
  handoffs/
  evals/
```

See `recipes/openai-agents-sdk.md` before adding runtime code.

