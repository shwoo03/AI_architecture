# opencode

## 기본 정보

- `name`: opencode
- `url`: https://github.com/anomalyco/opencode.git
- `source_type`: repository
- `status`: reviewing
- `searched_for`: provider-agnostic coding agent CLI, plan/build agents, LSP support, client/server architecture
- `created_at`: 2026-05-13
- `reviewed_at`: 2026-05-21
- `reviewer`: codex

## 왜 보는가

- `problem_statement`: 우리 스켈레톤은 Codex/Claude 중심이지만 장기적으로 provider/runtime 중립적인 agent 운영 모델도 고려해야 한다.
- `why_it_matters`: OpenCode는 open source AI coding agent로 provider-agnostic configuration, plan/build agents, general subagent, opt-in LSP, TUI/client-server architecture를 제시한다.
- `expected_value`: 역할 분리, LSP 연결, provider abstraction, client/server 경계, plan/read-only agent 정책을 우리 v2 설계와 비교할 수 있다.

## 유용한 패턴

- `useful_patterns`:
  - build agent와 read-only plan agent를 기본 역할로 분리한다.
  - general subagent를 complex search and multistep tasks에 내부적으로 사용한다.
  - provider-agnostic model/runtime configuration을 중요 가치로 둔다.
- `what_to_copy_conceptually`:
  - planner/read-only와 implementer/full-access를 분리하는 역할 경계.
  - LSP support를 opt-in diagnostic surface로 둔 점.
  - client/server architecture를 통해 TUI 외에도 여러 client를 허용하는 관점.
- `what_not_to_copy`:
  - OpenCode CLI/TUI/client-server runtime을 기본 의존성으로 도입하지 않는다.
  - install script, desktop app, package manager setup을 스켈레톤에 흡수하지 않는다.
  - provider configuration을 우리 config로 무리하게 복제하지 않는다.

## 증거

- `evidence_summary`: README identifies OpenCode as an open source AI coding agent with installation methods, desktop app, build/plan agents, general subagent, provider agnosticism, LSP support, and client/server architecture.
- `local_clone_path`: ../AI_architecture_references/opencode
- `checked_revision`: c64ac905e19cd881e4d3c8af6449f228941a2674
- `clone_present_at_review`: yes
- `freshness_signal`: 2026-05-21에 repo 밖 reference checkout의 HEAD를 확인했다.
- `maintenance_signal`: README includes package installers, desktop app, agents, docs, contribution, FAQ, and community links.
- `documentation_signal`: README links docs, agents, install paths, and multilingual documentation.
- `validation_signal`: Local README and license were inspected; no code copied.
- `sources`:
  - {"path":"../AI_architecture_references/opencode/README.md","kind":"readme","evidence":"Provider-agnostic CLI, role split, LSP, and client/server patterns were reviewed.","hash_or_line_ref":"local-reference"}
  - {"path":"../AI_architecture_references/opencode/LICENSE","kind":"license","evidence":"License boundary reviewed.","hash_or_line_ref":"local-reference"}

## 리스크

- `license`: MIT
- `security_or_privacy_risk`: Provider config, install scripts, and client/server runtime may touch credentials or local environment.
- `maintenance_risk`: Fast-moving CLI/runtime and package manager install paths can drift.
- `complexity_risk`: Full coding agent runtime is outside our skeleton's lightweight scope.
- `dependency_risk`: TUI/client/server/package ecosystem conflicts with Python stdlib-first scripts.
- `fit_risk`: Strong role and config reference, but direct code adoption is not appropriate.

## 적용 후보

- `applies_to`: docs | rules | skills | scripts | tests | runtime
- `target_files_or_areas`:
  - config/roles.yaml
  - scripts/lsp-diagnostics.py
  - scripts/agent-brief.py
  - docs/AGENT_REGISTRY.md
- `adoption_decision`: adapt
- `decision_reason`: Plan/build role separation and LSP/provider abstraction are directly useful, but runtime adoption is too broad. The planner/read-only and implementer/full-access boundary is already partly absorbed through `config/roles.yaml` and `scripts/agent-flow.py` write-policy handling; optional diagnostics have a local surface in `scripts/lsp-diagnostics.py`.
- `next_action`: Treat read-only CLI smoke and LSP diagnostic integration as a separate future proposal. Do not bundle OpenCode follow-up into the ECC Unicode/security provenance dry-run.

## 점수

| 기준 | 배점 | 점수 | 근거 |
| --- | ---: | ---: | --- |
| 문제 적합성 | 20 | 17 | Role split, LSP, and provider abstraction match our roadmap. |
| 구조 명확성 | 15 | 13 | README explains install, agents, docs, and architecture direction. |
| 검증 가능성 | 15 | 11 | Concepts are inspectable; runtime requires separate install. |
| 유지보수 신호 | 15 | 14 | Active install/docs/community signals are present. |
| 흡수 비용 | 15 | 11 | Concept reuse is cheap; runtime reuse is high. |
| 보안/라이선스 리스크 | 10 | 8 | MIT, but install/provider config need care. |
| 설명 가치 | 10 | 9 | Good comparison for role boundaries and LSP diagnostics. |
| 합계 | 100 | 83 |  |

## Dry-Run 제안

- `proposal_needed`: yes
- `files_to_change`:
  - config/roles.yaml
  - scripts/agent-brief.py
  - scripts/lsp-diagnostics.py
- `behavior_change`: Keep planner/read-only and implementer/full-access boundaries explicit while diagnostics stay optional.
- `validation_plan`: python scripts/validate-reference-candidates.py && python scripts/validate-reference-proposals.py && python scripts/quality-gate.py --skip-tests --format json
- `rollback_or_stop_condition`: Stop if changes imply adopting the full OpenCode runtime, install script, TUI, or provider config.
- `approval_required`: yes

## 최종 기록

- `final_status`: reviewing
- `implemented_in`:
  - not implemented
- `validation_result`: candidate card only
- `activity_log_entry`: not recorded
- `notes`: No code copied. Already absorbed portions: role/write-policy separation and an optional LSP diagnostics surface. Remaining candidate work: a narrow read-only public command smoke tier and possible doctor/diagnostic integration after a separate proposal.
