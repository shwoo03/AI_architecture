# Progress

## 현재 마일스톤
Component-level install/diff manifest와 skills/agents bloat audit slice completed (2026-05-29)

## 완료된 작업
- 2026-05-29 component-level install/diff manifest slice completed: release manifest now emits coarse install components, component file metadata, component generated artifact policy, and upgrade brief `component_diff`; `surface-bloat-audit.py` is documented as the read-only skills/agents bloat audit entrypoint. Focused upgrade tests, bloat audit tests, schema-check, verify-skeleton, release-manifest check, ownership-lock check, and closeout passed.
- 골격 부트스트랩
- LSP 진단과 source-scoped recovery
- AI-assisted upgrade brief, resume-safe checkpoint, knowledge gap check
- P0 안정화: no-record 무변경, read-only 라우팅, parity brief, WARN detail, recovery packet
- 운영 진단 개선: quality-gate explain, proposal lifecycle checker, progress-handoff lint
- reference inventory 정리: tracked repo, candidate card, proposal 상태 대조
- closeout hygiene: codemap 자동 갱신, repo-bounded pycache cleanup
- feature maturity manifest: stable/incubating/experimental tier source of truth
- tier-aware quality gate: stable default와 all-tier v2 검증 분리
- profile-aware upgrade brief: stable/incubating/all overlay 판단 지원
- v2 문서 경계: roadmap, design doc, SDK adapter ADR, JSONL source-of-truth ADR
- Phase 1a AgentBrief: Plan child artifact boundary, `--write` creator, canonical fields, manual-smoke brief artifact
- Phase 1a AgentRun draft: ledger required fields documented with `tier` and `ext`
- Phase 1b AgentBrief cleanup: true alias 제거, `policy_inheritance` 격리, `python3` validation hint 통일
- Phase 1b AgentRun writer: `scripts/incubating/agent-run.py add`로 manual smoke record append
- Phase 1b AgentRun reader: `dump --tail 5 --format json`으로 최근 ledger record 확인
- Phase 1b manual smoke: canonical brief 1건 생성 후 `runtime/agent-runs.jsonl`에 incubating run 1건 기록
- Phase 1c controlled migration: pre-v2 AgentRun 2건을 frozen `runtime/agent-runs.legacy.jsonl`로 분리
- Phase 1c read-side aggregation: `agent-run.py list/check/summary` 추가
- Phase 1c all-tier gate: `quality-gate --tier all`에서 incubating AgentRun schema check 연결
- Phase 1d-1 changed_paths validation: AgentRun add strict path validation, historical WARN check, all-tier WARN handling
- Phase 1d-2 retry/idempotency: explicit `retry_of`, same-brief live-ledger lookup, duplicate run id check, retry severity validation
- Phase 1d-3 aggregation summary: retry-aware `summary` fields for retried count, chain heads, unresolved failures
- Phase 1e incubating delegate entrypoint: `agent-brief.py`를 재사용해 brief artifact와 실행 가능한 handoff JSON을 생성
- Phase 1e-followup workflow-aware completion command: read-only workflow에서는 `--changed-path` placeholder를 생략하고 write workflow에서는 유지
- 0006 delegate cycle aborted on role-registry gap; AgentBrief artifact policy documented via operator housekeeping. Write-heavy friction goal remains OPEN, deferred to post-0008.
- 2026-05-15 freeze decision: 추가 슬라이스(0007 포함) 진행 보류. 외부 프로젝트 X 실 작업 발생 시 v1 stable overlay 시도 후 resume.
- 2026-05-15 ENKI_WIKI external trigger: stable overlay release path hardened, with migration limited to deterministic safe-only application.
- Ownership-aware upgrade v1 Slice 2: standalone classifier library and synthetic fixture tests implemented; closeout freshness cleanup in progress.
- Ownership-aware upgrade v1 Slice 2.5: skeleton self-classification lock generated; ownership-lock write/check and self-classification tests pass.
- Ownership-aware upgrade v1 Slice 3: verify-skeleton ownership gate and upgrade-from-skeleton ownership-aware planning implemented, validated, and closeout recorded.
- Ownership-aware upgrade v1 Slice 3.5: self-hygiene completed, OWNERSHIP_MODEL locked, ownership-initialize report tool implemented, tests pass, stable quality gate is WARN 0, closeout recorded, and plan moved to done.
- Slice 4 ENKI_WIKI initialize dry-run started with plan 0013. Read-only `ownership-initialize.py --target ~/mydir/ENKI_WIKI --format json` completed with status=draft, analyzed_paths=957, candidate_paths=261, and stop condition hit because candidates exceed 20. No ENKI target files were written.
- Slice 4 handoff/activity-log timestamp sync recorded after the initialize stop condition.
- Slice 4 first-milestone closeout recorded at 2026-05-15T12:36:45Z; next action remains manual review of the 261-path initialize draft before any ENKI target write.
- 2026-05-17 0013 ENKI ownership initialize dry-run marked done. The milestone completed read-only with normal stop on `candidate_paths=261`; follow-up classification remains a separate human-reviewed slice.
- 2026-05-16 v2 specialist overlay exception recorded for 0014+0015 only; plan 0014 specialist-overlay-audit is active, while 0016-0018 remain frozen until external usage signals appear.
- 2026-05-16 handoff/activity-log/session-snapshot refreshed after 0014 setup closeout.
- 2026-05-16 plan 0014 specialist overlay audit executed. `docs/_meta/SPECIALIST_OVERLAY_AUDIT.md` records current registry, brief, delegate, generated-agent, future 0017/0018 hook, and privilege-safety findings.
- 2026-05-16 plan 0014 moved to done and plan 0015 specialist-overlay-loader prepared as the active additive-only loader implementation slice.
- 2026-05-16 closeout for 0014 execution and 0015 preparation recorded successfully; validate-plans, verify-skeleton, ownership-lock check, stable quality gate, all-tier quality gate, and agent-flow closeout passed.
- 2026-05-16 0015 implementation prep recorded in `runtime/validation/0015-specialist-overlay-loader-prep.md`: exact code path, merge semantics, tests, docs, and security boundary are fixed before behavior changes.
- 2026-05-16 0015 implementation prep closeout recorded and handoff/session snapshot refreshed.
- 2026-05-16 0015 specialist overlay loader implemented: `agent-brief.py` reads optional `config/agent-team-overrides.yaml`, supports overlay-only project specialists, emits `role_source`, allows base narrowing, rejects base broadening, and delegate handoff carries role provenance.
- 2026-05-16 0015 specialist overlay loader closeout recorded successfully and plan moved to done.
- 2026-05-16 0016-0018 planning-only exception recorded. Active plan bodies now define the specialist proposal schema, orchestration preview `DelegationPlan` interface, and execution-loop boundary without changing scripts, schemas, CLI behavior, routing behavior, delegate behavior, or generated agent surfaces.
- 2026-05-16 0016-0018 planning-only handoff and session snapshot refreshed after closeout.
- 2026-05-16 specialist-agent usage best-practices prep completed. `docs/_meta/SPECIALIST_AGENT_USAGE_BEST_PRACTICES.md` now records source-grounded on-demand trigger/anti-trigger, orchestration, contract, safety, and implementation-readiness rules; 0016-0018 plans reference those rules.
- 2026-05-16 0016-0018 specialist flow implemented end to end: `agent-flow specialist propose/review/approve/reject`, preview `DelegationPlan`, and approval-gated execute now work through the existing delegate path without auto-spawning agents.
- 2026-05-16 0016-0018 moved to done; focused specialist tests, `tests.test_agent_flow`, combined operational tests, validate-plans, ownership-lock, verify-skeleton, stable/all quality gates, and closeout passed.
- 2026-05-16 0016-0018 implementation handoff/readiness timestamp sync refreshed after closeout and final strict-readiness pass.
- 2026-05-16 0019 session recall hardening implemented: session handoff is indexed, FTS cache text is redacted at insert time, cache index_version invalidates stale unredacted DBs, `agent-flow recall` wraps session recall search, and the SQLite cache is ignored as derived runtime data.
- 2026-05-16 0019 moved to done; focused recall tests, `tests.test_runtime`, `tests.test_agent_flow`, validate-plans, ownership-lock, verify-skeleton, stable/all quality gates, and closeout passed.
- 2026-05-16 0019 handoff/session snapshot refreshed after closeout.
- 2026-05-16 0020 specialist usage evidence ledger implemented: specialist preview/approve/reject/plan-approve/execute now append redacted advisory-only evidence records to `runtime/specialist-usage.jsonl` without changing scoring, auto-selection, validator verdicts, or spawn packet behavior.
- 2026-05-16 0020 moved to done; `tests.test_agent_flow`, validate-plans, ownership-lock, verify-skeleton, stable/all quality gates, and closeout passed.
- 2026-05-16 0021 closeout-validator loop extension implemented by explicit user exception: `agent-run.py validate` appends `closeout-validator` verdict records with `ext.validator`, additive statuses (`paused`, `aborted`, `partial`), and no mutation of target AgentRun records.
- 2026-05-16 0021 moved to done; focused validator tests, `tests.test_validation`, agent-run check, validate-plans, ownership-lock, verify-skeleton, stable/all quality gates, and closeout passed.
- 2026-05-16 0022 spawn-ready packet implemented by explicit user exception: `agent-flow specialist packet --plan <path> --confirm` writes harness-agnostic `runtime/spawn-packets/*.json` artifacts from approved `DelegationPlan` records and existing delegate handoffs.
- 2026-05-16 0022 moved to done; packet tests, `tests.test_agent_flow`, validate-plans, ownership-lock, generate-codemaps, verify-skeleton, stable/all quality gates, and closeout passed. Packet generation remains approval-gated and does not spawn agents, auto-chain roles, or allow recursive delegation.
- 2026-05-17 0023 diagnostic UX implemented: `agent-flow doctor` now aggregates `skeleton-doctor`, `verify-skeleton`, `ownership-lock check`, `resume-readiness --strict`, and tier-aware `quality-gate` into one read-only OK/WARN/FAIL report.
- 2026-05-17 0023 moved to done; focused doctor tests, `tests.test_agent_flow`, `agent-flow doctor` stable/all real runs, validate-plans, ownership-lock, generate-codemaps, verify-skeleton, all-tier quality gate, and closeout passed.
- 2026-05-17 0024 project adoption intake implemented: `agent-flow adopt --target <path>` now provides a read-only external-project adoption intake with `--status` and default dry-run modes only. It reports target cleanliness, skeleton revision hints, project profile state, license signal, safe missing/manual/risky upgrade counts, ownership candidate counts, recommendation, stop reasons, and next action without writing target files or adoption ledgers.
- 2026-05-17 0024 moved to done; focused adopt tests, full `tests.test_agent_flow`, ENKI read-only dry-run (`recommendation=stop`, `candidate_paths=263`), ENKI status check, validate-plans, ownership-lock, generate-codemaps, verify-skeleton, stable/all quality gates, and closeout passed.
- 2026-05-17 0025 ENKI ownership classification completed as a target-write proposal. Fixed non-ASCII git path collection (`core.quotePath=false`) after ENKI root file `주제` surfaced as a quoted path; ownership initialize now reports `candidate_paths=264`. A temporary ENKI copy with the proposed `project_overrides` wrote an ownership lock with `unknown=[]`, proving the classification proposal can clear the ownership unknown blocker without touching the real ENKI target.
- 2026-05-17 0025 moved to done; focused ownership tests, initialize report, temporary-copy simulation, validate-plans, ownership-lock, generate-codemaps, verify-skeleton, stable quality gate, and closeout passed. Real ENKI remains clean and unchanged.
- 2026-05-17 0026 ENKI ownership baseline and safe apply completed. ENKI target now has local commit `768e3a3e` for `config/ownership.yaml` + ownership lock baseline and local commit `a083da8f` for the remaining safe-only AI_architecture additions. ENKI working tree is clean and ahead of origin by 2 commits; no push was performed.
- 2026-05-17 0026 validation passed: ENKI ownership lock drift is 0, ENKI `verify-skeleton.py` passes, ENKI resume-readiness passes, session snapshot/recall are fresh, and ENKI quality gate passes with `--skip-tests --skip-node` (OK=28, WARN=2). Adoption intake now reports `safe_missing=0`, `candidate_paths=0`, `stop_reasons=[]`, and `recommendation=needs_review` due to remaining metadata/manual/risky review.
- 2026-05-17 0027 ENKI manual/risky system review completed. ENKI target now has local commits `a46bc3ea` (`Update AI operating OS system files`) and `54e8c33e` (`Align transferred system ownership rules`) on top of 0025/0026. ENKI is clean and ahead of origin by 4 commits; no push was performed.
- 2026-05-17 0027 validation passed: ENKI ownership lock drift is 0, ENKI `verify-skeleton.py` passes, ENKI `resume-readiness --strict` passes with WARN 0, ENKI stable quality gate with `--skip-tests --skip-node` passes (OK=34, SKIP=1, WARN=2), and final adoption intake reports `safe_missing=0`, `manual_merge=1`, `risky_changed=1`, `candidate_paths=0`, `target_git_clean=true`. Remaining differences are intentional preserves: ENKI `.github/workflows/ci.yml` and `config/ownership.yaml`.
- 2026-05-17 0027 AI closeout evidence and handoff were refreshed after `agent-flow closeout`; next-session resume state points to the intentional remaining ENKI differences rather than more bulk migration work.
- 2026-05-17 ENKI follow-up sync committed `7c64a566` (`Sync adoption intake profile check`) after the AI-side `PROJECT_PROFILE` readiness checker was narrowed to required-field placeholders only. ENKI is clean and ahead of origin by 5 local commits. Final intake now reports `project_profile_state=ready`, `safe_missing=0`, `manual_merge=1`, `risky_changed=1`, `candidate_paths=0`, `target_git_clean=true`; the remaining review signals are license unknown, already-initialized ownership, and the two intentional preserves (`.github/workflows/ci.yml`, `config/ownership.yaml`).
- 2026-05-17 ENKI migration follow-up cleanup completed: Karpathy reference tracking, deferred 0007 closure, surgical Karpathy concept-only rule/skill absorption, and ENKI retrofit case study documentation were committed with generated/local files excluded.
- 2026-05-17 0028 closeout wrapper timing completed in two commit boundaries. Commit A added append-only `runtime/closeout-timings.jsonl` instrumentation. Commit B rewired `agent-flow closeout` so non-`all` profiles delegate checks to `task-closeout.py` while `all`/baseline paths keep strict front checks. Measurement evidence shows docs-profile closeout P50 improved to roughly 1.6s.
- 2026-05-17 0029 adoption semantics completed. `agent-flow adopt` now reports `review_classification.blocking` vs `non_blocking_review`, treats unknown license as non-blocking metadata review, reads old install-state records with fallback, reports previous/current skeleton source commits, and lists intentional preserve candidates without writing target files.
- 2026-05-17 final handoff/progress refresh is in progress for the ENKI migration follow-up goal; push remains explicitly out of scope.
- 2026-05-17 final runtime-profile closeout recorded for the ENKI migration follow-up goal. Push was not performed.
- 2026-05-17 generated/local dirty policy cleanup implemented. `.claude/`, `.codex/`, `.mcp.json`, and `CLAUDE.md` are now ignored generated/local surfaces rather than tracked canonical files; local generated artifacts remain present and are regenerated by `scripts/convert.py`. Ownership lock, codemaps, install-state, closeout evidence, handoff, and resume-readiness metadata were refreshed.
- 2026-05-17 private near-original GitHub mirror policy implemented. Private project mirrors may now track generated runtime surfaces (`.claude/`, `.codex/`, `.mcp.json`, `CLAUDE.md`) and explicitly approved `.env` files, while `__pycache__/`, `node_modules/`, virtualenvs, build outputs, and rebuildable caches remain excluded. Bootstrap copy rules now allow generated surfaces/local config instead of unconditionally dropping them, while preserving skeleton-internal runtime history exclusions.
- 2026-05-17 closeout/test/validation loop cost reduction implemented. Recent auto closeouts were measured at about 59-61s; script/test edits now infer `scripts-fast`, which runs `verify-skeleton`, changed-file AST syntax checks, and focused unittest modules instead of the heavy scripts profile. `quality-gate.py` now runs independent checks with bounded parallelism (`--jobs 4` default; sequential debug via `--jobs 1`). Measured stable quality gate improved from about 7.31s sequential to 4.19s parallel; a one-file `scripts-fast` closeout smoke took about 10.70s, and this real multi-file closeout recorded 32.657s with focused tests.
- 2026-05-19 common skeleton hygiene/readiness improvements implemented through focused validation: bootstrap/upgrade now preserve Python cache ignore policy, bootstrap avoids and cleans bytecode cache drift, `verify-skeleton.py` reports tracked Python bytecode/cache files, `validate-plans.py --allow-legacy-done` handles structured historical done plans without weakening active-plan closeout enforcement, and `resume-readiness.py --strict` surfaces ownership lock drift unless explicitly deferred in handoff. Machine-local path policy remains a documented decision placeholder, not an implemented policy.
- 2026-05-19 final closeout for common skeleton hygiene/readiness improvements completed. Focused tests, `verify-skeleton`, `verify-parity`, strict resume-readiness, strict security scan, stable quality gate with `--skip-tests --skip-node`, ownership-lock check, session snapshot, session recall, and pycache cleanup all passed after final ordering cleanup.
- 2026-05-21 reference card carrier round completed before security gate implementation. The stale untracked 0001 plan carrier was preserved as done plan 0030, active plan 0031 documented the card-integrity scope, proposal lifecycle now supports `superseded`, and ECC/paperclip/opencode candidate cards were refreshed with scoped revision/source-anchor decisions. Unchecked candidate cards are listed in `research/reference-candidates/README.md`; no invisible-character gate, copy-ledger gate, closeout summary, or upgrade verify implementation was started.
- 2026-05-21 handoff timestamp alignment refreshed after skeleton verification; ownership lock drift remains explicitly deferred outside the card-integrity round.
- 2026-05-21 reference card carrier round closeout recorded with reference profile and plan 0031 moved to done. A scripts-fast closeout attempt exposed an unrelated Python 3.9 incompatibility in `tests.test_validation`; the focused proposal tests and reference validators passed.
- 2026-05-24 external reference refresh completed. All 8 sibling clones under `../AI_architecture_references` are clean and aligned with upstream after `git pull --ff-only --prune --stat`; `references.yaml` now records current HEADs, `last_checked`, and the previously candidate-carded `llm_wiki` registry entry.
- 2026-05-24 final validation rechecked after runtime record refresh: reference freshness, schema check, skeleton verification, and resume-readiness were brought back into alignment.
- 2026-05-27 Codex-Claude dialogue consensus first slice completed. `scripts/dialogue-lint.py` now validates `runtime/dialogues/*.jsonl`; `agent-flow dialogue start|add-turn|status|converge` records targeted claims, critiques, concessions, fallback identity, and convergence scope; unresolved block critiques prevent implementation convergence.
- 2026-05-27 fallback critic boundary implemented: real `claude` turns cannot use `provider=fallback_subagent`, and `claude-fallback-critic` turns must be labeled with `provider=fallback_subagent` plus a fallback reason. Automatic Claude CLI invocation and automatic subagent spawning remain out of scope for this slice.
- 2026-05-27 plan 0032 moved to done; focused dialogue tests, plan validation, schema check, ownership lock refresh, skeleton verification, all-tier quality gate, closeout, session snapshot, and session recall were refreshed.
- 2026-05-27 plan 0033 plan_id evidence trace completed. `agent-flow closeout --plan-id <id>` now passes the explicit plan id through `task-closeout.py` into completion evidence. Supplied ids must resolve to `plans/active/<id>.md` or `plans/done/<id>.md`; `plans/failed` is not accepted, and omitted ids preserve the old record shape.
- 2026-05-27 plan 0033 validation passed: focused plan-id tests, full `tests.test_agent_flow`, full `tests.test_validation`, plan validation, schema check, ownership lock refresh, skeleton verification, and closeout evidence with `plan_id=0033-plan-id-evidence-trace`.
- 2026-05-27 plan 0034 Claude strategy critic boundary completed. Real Claude dialogue records are now strategy/critique only: `from: claude` requires `provider=claude_cli`, records `role_boundary=strategy_critic_only`, records `subagents_allowed=false`, and `dialogue-lint.py` rejects real Claude subagent/delegation fields.
- 2026-05-27 plan 0034 validation passed: focused dialogue tests, full `tests.test_agent_flow`, full `tests.test_validation`, plan validation, schema check, ownership lock refresh, skeleton verification, and closeout evidence with `plan_id=0034-claude-strategy-critic-boundary`.
- 2026-05-27 plan `0001-delegation-permission-preflight-hardening` completed. `agent-flow specialist execute` and `specialist packet` now run a static DelegationPlan preflight before writing AgentBrief or spawn packet artifacts, reject unsupported DAG/auto/recursive fields, unknown/duplicate roles, repo-escaping scopes, and scope/policy broadening beyond specialist registry or parent constraints.
- 2026-05-27 delegation hardening validation passed: focused specialist tests, `tests.test_agent_flow`, `tests.test_validation.NewInternalToolTests`, full `python3 -m unittest discover -s tests -v` (334 tests), `verify-skeleton`, ownership lock refresh, and closeout evidence with `plan_id=0001-delegation-permission-preflight-hardening`.
- 2026-05-27 plan `0001-source-anchor-strict-mode` completed. Reference candidate/proposal validators and copied-source ledger checks now support optional `--strict-source-anchor`; skill lifecycle promotion/demotion proposals now include a `Source anchors` section tying recommendations to the skill path, usage ledger, lifecycle ledger, proposal queue, and policy source.
- 2026-05-27 source-anchor strict validation passed: focused reference/security and ledger tests, full `python3 -m unittest discover -s tests -v` (338 tests), default reference candidate/proposal/copy-ledger validators, skeleton verification, and closeout evidence with `plan_id=0001-source-anchor-strict-mode`. Running strict source-anchor checks against all current cards intentionally surfaces legacy weak anchors as future cleanup work; strict remains opt-in.
- 2026-05-29 subagent debate workflow implementation in progress. `agent-flow dialogue` and `scripts/dialogue-lint.py` have been changed from Claude/fallback readiness to Codex plus scoped `subagent-*` readiness; focused dialogue tests, full `tests.test_agent_flow`, and full `tests.test_validation` pass.
- 2026-05-29 subagent debate workflow closeout recorded with `plan_id=0001-subagent-debate-workflow-replaces-claude-auto-di`; handoff, session snapshot, session recall, and ownership lock were refreshed after closeout.
- 2026-05-29 plan `0001-codex-cli-goal-prompt-length-contract` completed. `goal 프롬프트` now means a Codex CLI `/goal` prompt capped at 4000 characters; over-limit details should be split into a separate document and referenced by the short prompt. `scripts/goal-prompt-check.py` validates the limit.
- 2026-05-29 plan `0001-versioned-skeleton-release-upgrade-support` completed. The skeleton now has `scripts/release-manifest.py` for stable/preview/edge release manifests; `upgrade-from-skeleton.py --brief` exposes release id/source commit/channel; safe-only apply appends `skeleton_release_applied` install-state provenance; and `agent-flow adopt` reports target/current release context while preserving old install-state fallback.
- 2026-05-29 external reference refresh completed again. All 8 sibling clones under `../AI_architecture_references` are clean and aligned with upstream; `references.yaml` now records 2026-05-29 HEADs for ECC, Hermes, llm_wiki, opencode, oh-my-codex, oh-my-claudecode, paperclip, and the unchanged Karpathy skills reference. Refresh recheck, schema check, skeleton verification, and handoff refresh passed.
- 2026-05-29 plan `0001-component-install-diff-surface-bloat-audit` completed. Release manifests now expose coarse install components plus generated artifact policy, upgrade briefs report component-level safe/manual/risky/protected counts, and `scripts/surface-bloat-audit.py` provides a read-only skills/agents surface drift audit. Full unittest discovery passed with 344 tests, stable quality gate passed, and closeout evidence records `plan_id=0001-component-install-diff-surface-bloat-audit`.
- 2026-05-29 plan `0001-feature-boundary-cleanup` completed. Stable features now distinguish `stable_role=core|advisory` and `delivery=overlay|frozen_optional|decision_only`; install profiles separate release components from project flavors; upgrade/release briefs preserve decision-only visibility without making it an apply target; root docs now point to canonical sources and generated surfaces instead of duplicating detailed subsystem policy.
- 2026-05-29 final readiness refresh for plan `0001-feature-boundary-cleanup`: closeout evidence recorded with matching `plan_id`, ownership lock checked clean, and session handoff/snapshot/recall refreshed.

## 다음 작업
- When closing explicit plans, pass `--plan-id <id>` to `agent-flow closeout`; do not infer plan ids from goal text.
- For critic-planning tasks, use subagent debate: start with `python3 scripts/agent-flow.py dialogue start --task "<task>" --format json`, add Codex claims plus `subagent-critic`/`subagent-researcher`/`subagent-verifier` critiques, and call `dialogue converge` only after blocking critiques are resolved and `ready_by` includes Codex plus at least one `subagent-*`. Do not use Claude automatic dialogue or Claude fallback participants.
- For `goal 프롬프트` requests, produce a Codex CLI `/goal` prompt under 4000 characters. If details would exceed that limit, write a separate document and make the `/goal` prompt point to it.
- For old-project skeleton upgrades, use release-aware adoption: `agent-flow adopt --target <repo> --format json`, review the release-aware `upgrade-from-skeleton --brief --profile stable` output, apply only approved `--safe-only`, regenerate generated artifacts, then verify parity/readiness/quality and record remaining manual/risky paths as intentional preserves.
- Use `python3 scripts/agent-flow.py adopt --target <legacy-project> --format json` before opening any external project adoption slice. Treat `review_classification.blocking=[]` with non-blocking reviews as an explicit human-review state, not as a file-transfer blocker.
- Use `python3 scripts/agent-flow.py doctor --format json` as the first single-command diagnostic before deeper manual checks. Use `--tier all` for incubating coverage and `--with-tests` for slower full validation.
- Use the new specialist flow only on demand: propose a project specialist when a concrete blocker/trigger exists, approve it before overlay application, preview delegation first, and execute only approved plans with explicit confirmation.
- Use `python3 scripts/agent-flow.py recall "<query>" --format json` for local session recall; source-of-truth remains Markdown/JSONL, and `runtime/session-recall.sqlite` is rebuildable cache only.
- Use `python3 scripts/incubating/agent-run.py validate --brief <closeout-validator-brief> --target-run-id <run-id> --verdict pass|warn|fail|needs_human --reason "<reason>" --format json` to append independent validator verdict evidence after specialist work.
- Use `python3 scripts/agent-flow.py specialist packet --plan <approved-delegation-plan.json> --confirm --format json` only when the user explicitly wants a spawn-ready handoff. The packet is an artifact for Codex/Claude/opencode/manual harnesses; the repo itself still does not spawn subagents.
- DelegationPlan execution now has a static safety preflight. If a plan contains dependency/DAG/auto-spawn/recursive-delegation fields or broadens role policy/scope, fix the plan rather than bypassing the check.
- Before adopting/copying/promoting from references, use the new opt-in source-anchor checks. Do not add these checks to global closeout until current reference cards have been refreshed with concrete anchors.
- ENKI migration next step: if the user accepts preserve-first semantics, treat operating OS transfer as complete. License selection, reference hygiene, and full ENKI Node/Docker validation remain optional follow-ups, not operating OS file transfer blockers.
- ENKI push remains out of scope until explicitly requested. Current ENKI target state is clean and ahead of origin by 5 local commits.
